-module(sdd_game).

%% API
-export([join/3]).

%%% =================================================================================== %%%
%%% API                                                                                 %%%
%%% =================================================================================== %%%

join(PlayerId, "BadGame", Source) -> error;
join(PlayerId, "GoodGame", Source) -> ok.